import { WowDirective } from './wow.directive';

describe('WowDirective', () => {
  it('should create an instance', () => {
    const directive = new WowDirective();
    expect(directive).toBeTruthy();
  });
});
