/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { VehicleInfo } from '../../models/vehicle-info';

export interface GetVehicleInfo$Params {
  vin: string;
}

export function getVehicleInfo(http: HttpClient, rootUrl: string, params: GetVehicleInfo$Params, context?: HttpContext): Observable<StrictHttpResponse<VehicleInfo>> {
  const rb = new RequestBuilder(rootUrl, getVehicleInfo.PATH, 'get');
  if (params) {
    rb.path('vin', params.vin, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<VehicleInfo>;
    })
  );
}

getVehicleInfo.PATH = '/api/v1/VINDecoder/decode/{vin}';
